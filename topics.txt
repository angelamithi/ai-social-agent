# Add one topic per line below. The agent reads and removes the TOP line
# each time it runs, so add new ones at the bottom (or top, your call) —
# order just determines which gets used next.
#
# Main topic is AI / AI agents. Blockchain, crypto, and tokenization are
# subtopics only — mention them when they're genuinely how an AI agent
# operates (e.g. agents transacting with each other using crypto), not
# as standalone crypto/market topics.
#
# Example (good — AI-led):
# How AI agents are starting to pay each other in crypto to complete tasks autonomously
# What it means that AI agents can now hold their own crypto wallets
#
# Lines starting with # are ignored.
